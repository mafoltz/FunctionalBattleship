public class Tasks {
	public void tasks() {
		/* TASKS DONE: */
		
		/* 1 - IMUTABLE FUNCTIONS*/
		/* 2 - LAMBDA FUNCTIONS */
		/* 3 - CURRYING (a -> b -> a + b) */
		/* 5 - HIGHER ORDER FUNCTIONS (Functions that receive functions as argument or return others functions) */
		/* 7 - FIRST CLASS FUNCTIONS (Functions can be used as parameters or returned as a subroutine result) */
	    /* 8 - RECURSION FOR ITERATION */
		
		
		
		/* TODO */
		
	    /* 4 - PATTERN MATCH */
	    // It needs to be implemented in Java
	    // https://kerflyn.wordpress.com/2012/05/09/towards-pattern-matching-in-java/
	    // https://stackoverflow.com/questions/11219858/java-8-pattern-matching
	    
	    /* 6 - MAP, REDUCE, FOLDR, FOLDL */
	    // foldr (+) 0 [1,2,3] -> 0+3+2+1 -> 6
	    //  map foo1 [2, 3, 6] -> [foo1(2), foo1(3), foo1(6)] -> [4, 6, 36]
	    //Collection<E> in = ...
	    	//Object[] mapped = in.stream().map(e -> doMap(e)).toArray();
	    	// or
	    	//List<E> mapped = in.stream().map(e -> doMap(e)).collect(Collectors.toList());
	}
}
